<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAppPengelolaKeungan"
        tools:targetApi="31">
        <activity
            android:name=".PengingatPembayaranActivity"
            android:exported="false" />
        <activity
            android:name=".TujuanKeuanganActivity"
            android:exported="false" />
        <activity
            android:name=".KelolaAkunActivity"
            android:exported="false" />
        <activity
            android:name=".AkunSayaActivity"
            android:exported="false" />
        <!--
        <activity
            android:name=".EmailVerivyActivity"
            android:exported="true">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!- Do not include other schemes.
        -->
        <!-- <data android:scheme="http" /> -->
        <!--
<data android:scheme="https" />

                <data android:host="sratus-23deb.firebaseapp.com" />
            </intent-filter>
        </activity>
        -->
        <activity
            android:name=".DaftarAkunActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>